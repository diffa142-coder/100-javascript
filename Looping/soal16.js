for (let i = 1; i <= 5; i++) {
    let spasi = ' '.repeat(5 - i);
    let bintang = '*'.repeat(2 * i - 1);
    console.log(spasi + bintang);
}
for (let i = 4; i >= 1; i--) {
    let spasi = ' '.repeat(5 - i);
    let bintang = '*'.repeat(2 * i - 1);
    console.log(spasi + bintang);
}